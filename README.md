# gpu-neural-net-accelerator
A C++ + CUDA project implementing matrix multiplication, ReLU activation, and softmax entirely on the GPU, benchmarked against CPU equivalents with measured speedup metrics. 
